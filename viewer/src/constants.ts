/** イベントボーナスの最大値（+0〜+12） */
export const MAX_EVENT_BONUS = 12;

/** イベントアイテム例: "ミトン(x3)" */
export const RE_EVENT_ITEM = /\(x(\d+)\)$/;

/** ポイント例: "ポイント(+600)" */
export const RE_POINT = /^ポイント\(\+(\d+)\)$/;

/** QP例: "QP(+150000)" */
export const RE_QP = /^QP\(\+(\d+)\)$/;
